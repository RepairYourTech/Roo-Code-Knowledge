.pnpm-store
dist
out
out-*
node_modules
package-lock.json
coverage/
mock/

.DS_Store

# IDEs
.idea

# Builds
bin/
*.vsix

# Local prompts and rules
/local-prompts

# Test environment
.test_env
.vscode-test/

# Docs
docs/_site/

# Dotenv
.env
.env.*
!.env.*.sample

# Logging
logs
*.log

# Vite development
.vite-port

# Turborepo
.turbo

# IntelliJ and Qodo plugin folders
.idea/
.qodo/
.vercel
.roo/mcp.json
.kilocode/mcp.json

# Qdrant
qdrant_storage/

# Tree-sitter and Tiktoken WASM files are committed to ensure reliable builds
# These files (~44MB total: 30 tree-sitter + 1 tiktoken) eliminate network dependencies
# Strategy: Commit WASMs to repo → Zero network deps in builds → Reliable CI/offline builds
#
# Directory structure:
#   src/wasms/tree-sitter/*.wasm  - 30 language parsers (~38MB)
#   src/wasms/tiktoken/tiktoken_bg.wasm - Token counter (~5-6MB)
#
# To populate/update WASMs:
#   Initial setup: pnpm setup-wasms-once (automated: downloads + copies + verifies)
#   Manual regeneration: pnpm regenerate-wasms && cp dist/services/tree-sitter/*.wasm src/wasms/tree-sitter/
#   Verify: ls -lh src/wasms/tree-sitter/*.wasm src/wasms/tiktoken/tiktoken_bg.wasm
#   Commit: git add src/wasms/ && git commit -m "chore: update WASM files"
#
# Force-include WASM files (override default .wasm ignore if any):
!src/wasms/**/*.wasm
!src/wasms/tree-sitter/*.wasm
!src/wasms/tiktoken/*.wasm
